# Contributing to HakureiTerminal

感谢你考虑为 HakureiTerminal 做贡献。本项目目前采用轻量治理：不强制签署 CLA，但要求所有贡献都满足本文件和 `DCO.md` 中的 Developer Certificate of Origin 1.1。

> 本文件不是法律意见。如你代表公司、学校、组织提交贡献，或贡献内容涉及雇佣关系、外包、商业授权、第三方素材，请先确认你有权提交。

## 许可证与贡献授权

- 仓库根项目 HakureiTerminal 采用 Apache License 2.0，见 `LICENSE`。
- GensokyoAI `2026.7.14.0` 是独立部署的唯一执行、会话、消息和上下文权威；HakureiTerminal 仅是其专用网络前端。许可证说明见 `THIRD_PARTY_LICENSES.md`。
- 本仓库默认不接受 GensokyoAI 本体源码修改；相关贡献应提交到 GensokyoAI 上游项目。
- 不得通过依赖、生成脚本、Flutter/Android assets、安装器或构建步骤把 GensokyoAI 本体、角色、场景、配置、文档、品牌素材、Python runtime 或 bridge 预置到 HakureiTerminal 源码和默认分发包。允许提交的互操作代码只能依赖文档化的公开网络契约。
- 不得增加本地聊天运行时、直接 Provider 调用、本地权威会话/消息/上下文或断线执行 fallback。断线客户端只能管理配置、非可执行角色草稿和一次性展示缓存，不能聊天。
- 角色草稿的编辑、导入、恢复和应用启动不得联网或上传。当前不接受角色上传实现；未来只有存在文档化公开网络 API，并提供明确目标连接和 payload 范围确认时才可设计。
- 除非某个文件、目录或维护者书面说明另有许可证，向本仓库提交贡献即表示你同意：你的贡献按其所提交到的项目或组件现有许可证授权。
- 对 HakureiTerminal 主项目的贡献默认按 Apache License 2.0 授权。
- 你保留自己贡献中的版权；你授予项目维护者和下游用户按对应开源许可证使用、复制、修改、分发、再授权和发布该贡献的权利。

## DCO 要求

所有提交应包含 Signed-off-by 行，表示你同意 `DCO.md` 中的 Developer Certificate of Origin 1.1。

推荐使用：

```cmd
git commit -s
```

提交信息末尾应包含类似内容：

```text
Signed-off-by: Your Name <you@example.com>
```

如果某个 PR 中存在未签署提交，维护者可以要求补签、压缩提交或重新提交。

## 贡献内容要求

提交贡献前，请确认：

- 贡献是你原创的，或你有权按本项目对应许可证提交。
- 贡献不包含你无权公开的公司代码、商业机密、受保密协议限制的内容。
- 贡献不复制粘贴不兼容许可证的第三方代码、文本、图片、音频、模型权重或数据集。
- 如引入第三方依赖、素材或大段参考实现，请在 PR 中说明来源、许可证和必要的归属声明。
- 不提交真实 API Key、Token、Cookie、密码、个人隐私数据或生产环境配置。

## PR 建议流程

1. Fork 仓库并创建主题分支。
2. 让每个 PR 聚焦一个问题或功能。
3. 更新相关文档、配置或测试。
4. 确认提交包含 Signed-off-by。
5. 在 PR 描述中说明变更目的、测试情况、许可证或第三方来源影响。

## 维护者处理原则

维护者可以因以下原因要求修改或拒绝贡献：

- 缺少 Signed-off-by。
- 无法确认贡献来源或授权。
- 引入许可证不兼容、版权归属不清或商业机密风险。
- 与项目边界、架构方向或质量要求不一致。

## 关于未来商业化或许可证策略变化

目前项目不强制 CLA，也不要求版权转让。DCO 适合当前个人项目和公开收 PR 的轻量治理。

如果未来项目需要双许可证、闭源商业版本、商标授权、企业级分发或更复杂的商业合作，维护者可能只对未来贡献引入额外协议。届时应在接受相关贡献前更新本文件、PR 模板或单独的协议流程，并避免对既有贡献做事后追认式处理。
