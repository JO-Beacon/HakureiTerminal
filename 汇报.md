# HakureiTerminal 专用前端迁移汇报

更新时间：2026-07-26

## 产品方向

HakureiTerminal 已转向独立部署的 `GensokyoAI 2026.7.14.0` 的专用 Flutter 前端。GensokyoAI 是可执行角色、聊天会话、消息、上下文、记忆、场景、工具、定时器和生成状态的唯一权威。

HakureiTerminal 不提供第一方或内置聊天运行时，不直接调用模型 Provider，也不会在 Runtime 断开、缺失或拒绝请求时本地执行或降级。断线应用仍可管理设置、非可执行角色草稿、媒体、惰性遗留数据和一次性展示缓存，但不能聊天。

## 已完成的本地实施

- 已移除 GensokyoAI 源码、Python bridge、嵌入式 Python、安装/下载/启动流程、本地角色部署和相关第三方 payload。
- 已实现 HakureiTerminal 自有的 HTTP/WebSocket 客户端边界，目标为 `GensokyoAI 2026.7.14.0`、Runtime protocol major version `1`。
- 保存连接、应用启动、草稿编辑/导入/恢复和 `.jovarchive` 导入均不会连接服务。连接、测试和 Provider 委托必须由用户明确操作。
- 模型 profile 仅保留用于明确委托给用户选定的 GensokyoAI Runtime，永不用于 HakureiTerminal 直接调用 Provider。
- Runtime 角色、会话、消息和其他执行状态仅保存远端 ID、只读展示摘要或一次性缓存，不作为本地权威状态。
- 源码和发布产物 scanner 用于阻止 GensokyoAI package/wheel/metadata、bridge、Python runtime、第三方角色/场景和旧默认配置重新进入仓库或分发物。

## 草稿与遗留数据

- 客户端创作角色是本地、非可执行草稿。编辑、导入、恢复或应用启动不会上传草稿。
- 当前没有角色上传功能。未来只有 GensokyoAI 提供文档化公开网络 API 后才能设计上传，并且必须通过独立用户操作明确显示目标连接和 payload 范围。
- 旧本地会话、消息、上下文和 Runtime 数据保持惰性，不进入生成上下文，不自动映射到远端会话，也不上传。
- `.jovarchive` 导入在本地校验；恢复的连接保持禁用并清除 Provider 委托。归档不能替代 GensokyoAI 自身的数据备份、恢复或删除机制。

## 安全与归档

- `%APPDATA%/HakureiTerminal/settings.json` 和完整 `.jovarchive` 可包含 Provider API Key、Runtime token、Runtime URL 和用户内容，必须作为敏感凭据备份保护，不能公开分享。
- Provider 委托只发送给用户确认的 Runtime 实例。撤销可阻止后续初始化发送，但不能远程擦除外部进程已经收到的秘密。
- 非 loopback Runtime 应使用 HTTPS/WSS、强 token、反向代理、防火墙和适当访问控制；部署安全、日志、备份和删除由 Runtime 运维方负责。

## 构建与扫描

当前准确的开发和发布验证入口以 `README.md` 为准：

```cmd
flutter pub get
flutter analyze
flutter test
python -m unittest -v test_scan_forbidden_runtime_assets.py
python scripts/scan_forbidden_runtime_assets.py source .
flutter build windows --release
flutter build apk --release
python scripts/scan_forbidden_runtime_assets.py artifact build\windows\x64\runner\Release
python scripts/scan_forbidden_runtime_assets.py artifact build\app\outputs\flutter-apk\app-release.apk
```

Windows release 默认输出到 `build\windows\x64\runner\Release\`，Android release APK 默认输出到 `build\app\outputs\flutter-apk\app-release.apk`。不存在旧 `scripts/build_windows_release.py`、内置 CPython 或 GensokyoAI 安装产物的发布要求。

## 状态与剩余验证

专用前端方向的本地代码、测试夹具、迁移、文档和 scanner 实现已完成。该结论与真实服务 operational validation 严格区分：当前仍需使用独立部署的真实 `GensokyoAI 2026.7.14.0` 验证公开协议、认证、聊天、事件和由 Runtime 执行的 Provider 流程，并对 Windows/Linux/Android release 候选物验证启动、断线行为、网络、签名和 artifact 扫描。

真实服务验证失败时应修正公开协议互操作或部署问题，不能恢复本地 Runtime、直接 Provider 调用、本地权威聊天数据或断线执行 fallback。
